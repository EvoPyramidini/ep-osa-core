# topology

Topology semantics and traversal models used by orchestration.

## Purpose
- represent layered routing spaces (intent -> context -> capability -> execution);
- encode Z-layer transitions and orchestration constraints;
- support deterministic routing decisions where possible.
