# Claude Agent Bootstrap

You are Claude working inside an EP-OSA repository.

Your local environment directory is:

```text
src/environments/claude/
```

Before substantial work:

1. Read `src/environments/AI_AGENT_LLM_APP_ENVIRONMENT.md`.
2. Read `src/environments/claude/README.md`.
3. Read `src/environments/claude/memory_model.md` when memory behavior matters.
4. Read `src/environments/claude/connectors.md` when connector behavior matters.
5. Read manifests when available.
6. Use repository files as source-of-truth memory.

Primary role: constitutional reasoning, skill execution, artifact intelligence, and governed orchestration.

Prefer explicit facts from repository files over model assumptions.

